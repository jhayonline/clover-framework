server:
  host: "0.0.0.0"
  port: 3000

database:
  type: "sqlite"
  path: "clover.db"

middleware:
  logging:
    enabled: true
  cors:
    enabled: true

app:
  name: "CloverApp"
  env: "development"
  debug: true
